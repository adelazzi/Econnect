import 'package:flutter/material.dart';

class gamerlist extends StatelessWidget {
  const gamerlist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
